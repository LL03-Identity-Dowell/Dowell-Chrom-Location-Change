/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/Dowell-Chrome-Location-Change', // Replace <repository-name> with your actual repository name
  assetPrefix: '/Dowell-Chrome-Location-Change', // Replace <repository-name> with your actual repository name
  output: 'export',
};

export default nextConfig;
